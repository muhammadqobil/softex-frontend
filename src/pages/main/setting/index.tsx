import React, {FC} from "react";

export const Settings: FC = (): React.JSX.Element => {

    return (
        <div style={{ padding: "10px" }}>
            <h2>Settings page</h2>
        </div>
    )
}